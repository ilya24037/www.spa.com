// Filament Alpine.js configuration
import persist from '@alpinejs/persist'

// Регистрируем плагин persist для Alpine
window.Alpine.plugin(persist)

// Экспортируем для использования
export default persist