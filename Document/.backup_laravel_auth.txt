@echo off
:: Название базы данных
set DB_NAME=laravel_auth

:: Путь к папке, где будет сохранён бэкап
set BACKUP_PATH=C:\www.spa.com\Document\backup

:: Имя файла с датой и временем
set FILE_NAME=%DB_NAME%_backup_%DATE:/=-%_%TIME::=-%.sql

:: Удаляем символы, которые Windows не любит
set FILE_NAME=%FILE_NAME: =_%
set FILE_NAME=%FILE_NAME:.=%
set FILE_NAME=%FILE_NAME:,=%

:: Полный путь
set FULL_PATH=%BACKUP_PATH%\%FILE_NAME%

:: Запуск дампа
echo 🔄 Резервное копирование в "%FULL_PATH%"
mysqldump -u root -p %DB_NAME% > "%FULL_PATH%"

:: Сообщение об окончании
echo ✅ Бэкап завершён: %FULL_PATH%
pause
